// Toggle flags for optional features.

export const FEATURES = {
  // Allow doctors to update their own profiles (on/off)
  ALLOW_DOCTOR_PROFILE_UPDATE: true,

  // Allow doctors to update appointment status themselves (on/off)
  ALLOW_DOCTOR_APPOINTMENT_STATUS_UPDATE: true,

  // Allow admins to edit medical records (otherwise admin is read‑only)
  ALLOW_ADMIN_MEDICAL_RECORD_WRITE: false,
};
